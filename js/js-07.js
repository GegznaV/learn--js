// https://youtu.be/9M4XKi25I2M 
// 38:13 - IIFE, Scope, and Window Object

(function() {
    // IIFE - immediately invoked function expression
})();


(function() {
    // IIFE - immediately invoked function expression

    // var makes variable local; e.g., accessible just inside the function
    var age = 5;

    // without "var" the variable is accessible outside the function scope
    // i.e., it is global
    mu = 10;
    
})();
